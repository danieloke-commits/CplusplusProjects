# Task Manager (C++)

A simple command-line **task manager** written in C++.  
It allows you to add, remove, mark tasks as completed, and persist them to a text file between program runs.

---

## ✨ Features
- Add new tasks  
- Remove tasks  
- Mark tasks as completed  
- List all tasks  
- Save tasks to a file (`Tasks.txt`)  
- Load tasks automatically on startup  

Compile Instructions

g++ -std=c++23 TaskManagerProject.cpp -o Task

./Task
